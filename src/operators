
1: Arithmetic operators
   + addition
   - sub
   * mul
   / div
   % reminder

2: Relational operators
   > more then
   < less then
   >= more then equals to
   <= less then equals to
   == equals to
   != not equals to

3: Logical operators
    && AND
    || OR
    ! NOT

4: unary operator
            Increment       decrement
poatfix     a++/a = a+1     a--/a = a-1

prefix       ++a/a+1 = a      --a/a-1 = a

Q1 : 5 subjects -> show Total Marks -> show Percentage
Q2 : simple intrest = Principle, Rate of intrest, Timeperiod